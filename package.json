{
  "name": "resume-tailoring-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Tailors a resume to a job description via the OpenAI GPT API and fills a Google Docs template without touching layout.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "tailor": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "openai": "^7.4.0"
  }
}
